import { ClienteCadastroDto } from './cliente-cadastro-dto';

describe('ClienteCadastroDto', () => {
  it('should create an instance', () => {
    expect(new ClienteCadastroDto()).toBeTruthy();
  });
});
