import { EventoCadastroDto } from './evento-cadastro-dto';

describe('EventoCadastroDto', () => {
  it('should create an instance', () => {
    expect(new EventoCadastroDto()).toBeTruthy();
  });
});
